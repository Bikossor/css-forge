import { IColor } from "../Interface/IColor";
export declare const rgb: (red: number, green: number, blue: number) => IColor;
export declare const rgba: (red: number, green: number, blue: number, alpha: number) => IColor;
export declare const hsl: (hue: number, saturation: number, lightness: number) => IColor;
export declare const hsla: (hue: number, saturation: number, lightness: number, alpha: number) => IColor;
